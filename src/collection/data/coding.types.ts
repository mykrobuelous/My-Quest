/* ===================================================================== */

import type { IDBrand } from '@/shared/types/utilTypes';

export type CodingType = {
     id: IDBrand;
     subject: string;
     duration: number; // in minutes
};
